namespace RayTracer.Fonts;

/// <summary>
/// This enumeration notes the supported font weights.  Note that we stick with those
/// values supported by Google fonts.
/// </summary>
public enum FontWeight
{
    Thin = 100,
    Light = 300,
    Regular = 400,
    Medium = 500,
    Bold = 700,
    Black = 900
}
